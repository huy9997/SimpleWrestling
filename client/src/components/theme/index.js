import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: "#04395E", //dark blue
    secondary: "#a3bac3" //grey
  },
  status: {
    danger: "red"
  }
});

export default theme;
